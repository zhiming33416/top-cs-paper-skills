# Supplied manuscript excerpt

The paper claims that StreamRank is more efficient than the baseline. Table 3 reports 18 ms latency for StreamRank and 27 ms for the baseline. The excerpt does not state hardware, precision, batch size, concurrency, throughput, warm-up, retrieval quality, or whether the measurements share an operating point.
