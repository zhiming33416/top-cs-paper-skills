# Polar Summary

Use only for a coarse profile of three to eight diagnostics normalized to a common `[0,1]` interpretation. Require axis and value columns.

- Preserve declared axis order across methods and revisions.
- State normalization and direction for every spoke.
- Use the same radial limits for all compared profiles.
- Do not compare raw metrics with different units.
- Prefer aligned intervals or small multiples for precise differences.

Caption obligations define normalization, direction, and diagnostic purpose. QA rejects values outside `[0,1]`, too few/many axes, missing normalization disclosure, and use as a benchmark ranking substitute.
