# Claim-strength calibration

Use `demonstrates` only for direct, robust evidence under the stated conditions. Use `shows` for clear observations, `supports` when evidence contributes to a claim, `suggests` for incomplete or indirect evidence, and `is consistent with` when alternatives remain. Preserve causal verbs only when design supports causal inference. Replace absolute quantifiers with tested scope.
